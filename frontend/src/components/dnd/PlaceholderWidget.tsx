import { Box } from '@mui/material';

export const PlaceholderWidget = () => {
    return (
        <Box
            sx={{
                width: '100%',
                height: 200,
                backgroundColor: 'rgba(47, 46, 46, .6)',
                borderRadius: 2,
            }}
        />
    );
};
